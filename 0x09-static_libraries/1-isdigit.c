#include "main.h"

/**
 * _isdigit - it checks for a digit (0 to 9)
 * @c: int that will be checked
 * Return: 1 if c is a digit and 0 if not
 */

int _isdigit(int c)

{
	return (c >= '0' && c <= '9');
}
